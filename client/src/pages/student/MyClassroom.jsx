import ResourceFeed from '../../components/ResourceFeed';

const MyClassroom = () => {
    return (
        <ResourceFeed
            type="batch"
            title="My Classroom"
            subtitle="Resources shared specifically with your batch."
        />
    );
};

export default MyClassroom;
