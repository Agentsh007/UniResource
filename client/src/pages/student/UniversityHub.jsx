import ResourceFeed from '../../components/ResourceFeed';

const UniversityHub = () => {
    return (
        <ResourceFeed
            type="global"
            title="University Hub"
            subtitle="Public resources shared by faculty across the university."
        />
    );
};

export default UniversityHub;
